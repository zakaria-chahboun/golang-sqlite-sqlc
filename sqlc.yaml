version: 2
sql:
  - engine: "sqlite"
    schema: "models/schema"
    queries: "models/query"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        json_tags_case_style: "snake"
        emit_prepared_queries: true
